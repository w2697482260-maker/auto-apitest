## 📍 项目位置

**完整路径**: `C:\Users\26974\esop-api-automation`

## 📊 项目统计

- ✅ **34个Python文件** - 完整的框架代码
- ✅ **6个YAML配置文件** - 环境、用户、业务配置  
- ✅ **2个Shell脚本** - 自动化执行
- ✅ **完整的文档** - README + 快速指南

## 📁 目录结构

```
C:\Users\26974\esop-api-automation\
│
├── config/                      # 配置文件
│   ├── env_config.yaml         # 环境配置 ⭐ 需要修改URL
│   ├── user_config.yaml        # 用户配置 ⭐ 需要修改账号
│   └── business_config.yaml    # 业务配置
│
├── utils/                       # 工具层（7个文件）
│   ├── http_client.py          # HTTP请求封装
│   ├── logger.py               # 日志工具
│   ├── assertions.py           # 断言工具
│   ├── data_handler.py         # 数据生成器
│   └── feishu_notifier.py      # 飞书通知
│
├── api/                         # API层（12个文件）
│   ├── bos/                    # BOS端API
│   ├── admin/                  # Admin端API
│   └── staff/                  # Staff端API
│
├── business/                    # 业务层（4个文件）
│   ├── hk_us_stock.py          # 港美股业务
│   ├── unlisted_stock.py       # 未上市业务
│   └── a_stock.py              # A股业务
│
├── testcases/                   # 测试用例（8个文件）
│   ├── conftest.py             # pytest配置
│   ├── bos/                    # BOS端测试
│   ├── admin/                  # Admin端测试
│   └── staff/                  # Staff端测试
│
├── testdata/                    # 测试数据
├── reports/                     # 测试报告
├── scripts/                     # 执行脚本
│   ├── run_tests.sh            # 测试执行
│   └── clear_reports.sh        # 清理报告
│
├── pytest.ini                   # Pytest配置
├── requirements.txt             # Python依赖
├── Jenkinsfile                  # Jenkins配置
└── README.md                    # 项目文档
```

## 🚀 快速开始（3步）

### 1️⃣ 安装依赖
```bash
cd C:\Users\26974\esop-api-automation
pip install -r requirements.txt
```

### 2️⃣ 修改配置
编辑 `config/env_config.yaml`，修改测试环境的URL：
```yaml
test:
  bos:
    base_url: https://你的BOS地址
  admin:
    base_url: https://你的Admin地址
  staff:
    base_url: https://你的Staff地址
```

编辑 `config/user_config.yaml`，修改测试账号。

### 3️⃣ 运行测试
```bash
# 冒烟测试
pytest -m smoke

# BOS端测试
pytest -m bos

# 港美股测试
pytest -m hk_us

# 查看详细输出
pytest -v -s
```

## 📝 常用命令

```bash
# 测试执行
pytest                          # 运行所有测试
pytest -m smoke                 # 冒烟测试
pytest -m "bos and hk_us"      # BOS端港美股
pytest -m p0                    # 高优先级
pytest -n 4                     # 4进程并行

# 生成报告
allure generate reports/allure-results -o reports/allure-report --clean
allure open reports/allure-report

# 查看日志
tail -f reports/logs/all_*.log
```

## 🎯 测试标记

- **端标记**: `bos`, `admin`, `staff`
- **业务线**: `hk_us`, `unlisted`, `a_stock`
- **优先级**: `smoke`, `p0`, `p1`, `p2`

## 📞 下一步

1. ⭐ 修改 `config/env_config.yaml` 中的URL
2. ⭐ 修改 `config/user_config.yaml` 中的测试账号
3. 根据实际接口调整API封装（api/目录）
4. 运行测试验证
5. 配置Jenkins集成
6. 配置飞书通知

---
**创建时间**: 2024-09-19  
**框架版本**: 1.0.0  
**技术栈**: Python + pytest + requests + allure + jenkins
